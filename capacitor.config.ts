import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.rcc.starter',
  appName: 'RCC',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
